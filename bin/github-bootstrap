#!/bin/bash -x

curl -L https://raw.githubusercontent.com/uperl/cip/main/bin/cip > /tmp/cip
chmod +x /tmp/cip
sudo mv /tmp/cip /usr/bin
md5sum /usr/bin/cip
sudo chown -R 1000:1000 .
sudo chown -R $(id -u):$(id -g) .git
