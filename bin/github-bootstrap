#!/bin/bash -x

curl https://raw.githubusercontent.com/plicease/cip/main/bin/cip > /tmp/cip
chmod +x /tmp/cip
sudo mv /tmp/cip /usr/bin
md5sum /usr/bin/cip
sudo mkdir -p ~/.cip
sudo chown -R 1000:1000 ~/.cip
sudo chown -R 1000:1000 .
